@model IEnumerable<MVCTemplate.Models.Tamaños>

@{
    ViewBag.Title = "Tamaños";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1 class=" text-center" style="font-weight:500; font-family: 'Pattaya', sans-serif;">Tamaños</h1>

<p>
    @Html.ActionLink("Crear nuevo", "Create")
</p>
<table id="DT" class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Tamaño)
            </th>
            <th>
                opciones
            </th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Tamaño)
                </td>
                <td>
                    @Html.ActionLink("Edit", "Edit", new { id = item.IDtamaño }) |
                    @Html.ActionLink("Delete", "Delete", new { id = item.IDtamaño })
                </td>
            </tr>
        }
    </tbody>
</table>
<script>
    $(document).ready(function () {
        $('#DT').DataTable({
            dom: 'Bfrtip',
            buttons: [
                'copy', 'excel', 'pdf'
            ]
        });
    });
</script>
